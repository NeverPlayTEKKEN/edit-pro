/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
          SITE_NAME: 'ClipRiZe',
          SITE_DESCRIPTION: 'あなたの配信をバズらせる切り抜き動画編集',
        },
};

export default nextConfig;
